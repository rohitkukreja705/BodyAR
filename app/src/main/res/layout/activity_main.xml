<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- ===================== Main camera + AR content ===================== -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/black">

        <androidx.camera.view.PreviewView
            android:id="@+id/preview_view"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            app:scaleType="fillCenter" />

        <com.hftx.bodyar.PoseOverlayView
            android:id="@+id/pose_overlay"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />

        <!-- Top bar -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:padding="12dp">

            <ImageButton
                android:id="@+id/btn_menu"
                android:layout_width="44dp"
                android:layout_height="44dp"
                android:background="#66000000"
                android:src="@drawable/ic_menu"
                android:contentDescription="@string/open_sidebar"
                android:padding="10dp" />

            <TextView
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:gravity="center"
                android:text="@string/app_name"
                android:textColor="@color/white"
                android:textStyle="bold"
                android:textSize="16sp" />

            <ImageButton
                android:id="@+id/btn_clear_all"
                android:layout_width="44dp"
                android:layout_height="44dp"
                android:background="#66000000"
                android:src="@drawable/ic_clear"
                android:contentDescription="@string/clear"
                android:padding="10dp" />
        </LinearLayout>

        <!-- Hint when no person is detected -->
        <TextView
            android:id="@+id/txt_hint"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal|top"
            android:layout_marginTop="70dp"
            android:background="#99000000"
            android:paddingHorizontal="14dp"
            android:paddingVertical="8dp"
            android:text="@string/no_person_detected"
            android:textColor="@color/white"
            android:textSize="12sp"
            android:visibility="gone"
            tools:visibility="visible" />

        <!-- Bottom capture bar -->
        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom"
            android:padding="24dp">

            <ImageButton
                android:id="@+id/btn_capture"
                android:layout_width="72dp"
                android:layout_height="72dp"
                android:layout_gravity="center"
                android:background="@drawable/bg_capture_button"
                android:src="@drawable/ic_camera"
                android:contentDescription="@string/capture"
                android:padding="18dp" />
        </FrameLayout>
    </FrameLayout>

    <!-- ===================== Sidebar wardrobe drawer ===================== -->
    <LinearLayout
        android:layout_width="300dp"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:orientation="vertical"
        android:background="@color/sidebar_bg"
        android:elevation="8dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:padding="16dp">

            <TextView
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="Wardrobe"
                android:textColor="@color/text_primary"
                android:textStyle="bold"
                android:textSize="18sp" />

            <ImageButton
                android:id="@+id/btn_close_drawer"
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:background="@android:color/transparent"
                android:src="@drawable/ic_close"
                android:contentDescription="@string/open_sidebar" />
        </LinearLayout>

        <!-- Category chips -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:paddingHorizontal="12dp"
            android:paddingBottom="8dp">

            <TextView
                android:id="@+id/chip_shirts"
                style="@style/CategoryChip"
                android:text="@string/shirts" />

            <TextView
                android:id="@+id/chip_pants"
                style="@style/CategoryChip"
                android:text="@string/pants" />

            <TextView
                android:id="@+id/chip_glasses"
                style="@style/CategoryChip"
                android:text="@string/sunglasses" />
        </LinearLayout>

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_add_photo"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginHorizontal="16dp"
            android:layout_marginBottom="8dp"
            android:text="+ Add your own photo"
            app:icon="@drawable/ic_camera"
            app:iconTint="@color/black"
            app:backgroundTint="@color/accent"
            android:textColor="@color/black"
            android:textAllCaps="false" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginHorizontal="16dp"
            android:layout_marginBottom="8dp"
            android:text="Tip: transparent PNG cutouts of real garments look best. Long-press an upload to remove it."
            android:textColor="@color/text_secondary"
            android:textSize="10sp" />

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recycler_items"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:clipToPadding="false"
            android:paddingBottom="12dp" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_clear_category"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_margin="16dp"
            android:text="@string/clear"
            app:backgroundTint="@color/chip_unselected"
            android:textColor="@color/text_primary" />

    </LinearLayout>

</androidx.drawerlayout.widget.DrawerLayout>
